---
import Base                    from '../../layouts/Base.astro';
import Nav                     from '../../components/Nav.astro';
import FeatureBlock            from '../../components/FeatureBlock.astro';
import Footer                  from '../../components/Footer.astro';
import SectionEyebrow          from '../../components/SectionEyebrow.astro';
import KnotArchitectureDiagram from '../../components/KnotArchitectureDiagram.astro';
import '../../styles/global.css';

// Desktop-app screenshots for the showcase carousel (see "THE DESKTOP APP"
// section below). Mirrors Heart's phone-mockup gallery, but framed in a desktop
// window because Knot is a desktop app, not mobile.
const screenshots = [
  { src: '/screenshots/knot/dashboard.png', caption: 'Dashboard — lifetime commits, devices, storage, bridge health',  alt: 'Knot desktop dashboard with a total-commits counter and device, storage and bridge-health panels' },
  { src: '/screenshots/knot/timeline.png',  caption: 'My Folders — per-project commit history & activity heatmap',     alt: 'Knot project view showing a commit activity heatmap and full commit history timeline' },
  { src: '/screenshots/knot/graph.png',     caption: 'Code graph — see how any codebase fits together',                alt: 'Knot code dependency graph showing modules as nodes radiating from a central node' },
  { src: '/screenshots/knot/activity.png',  caption: 'Activity — watch your AI agents coordinate across machines',      alt: 'Knot activity feed showing AI agents asking and answering across devices' },
  { src: '/screenshots/knot/chat.png',      caption: 'Built-in chat — message your paired devices directly',           alt: 'Knot chat tab showing a conversation between paired devices' },
  { src: '/screenshots/knot/settings.png',  caption: 'Settings — themes, on-device AI, go-dark, auto-save timing',      alt: 'Knot settings panel with theme, language, offline AI and notification options' },
];
---

<Base
  title="Knot · Peer-to-peer version control for the AI era"
  description="Take any folder on your machine. Every save becomes a real git commit and reaches your teammates within seconds — no account, no central server, no four-step push ritual. Peer-to-peer over Tor: works wherever normal web traffic does."
>
  <Nav />

  <!-- ── HERO (Knot-specific, slim) ──────────────────────────────────────── -->
  <section class="relative pt-32 pb-20 px-6 overflow-hidden">
    <div aria-hidden="true" class="absolute top-0 left-1/2 -translate-x-1/2 w-[80%] h-[28rem] bg-accent/[0.08] blur-3xl pointer-events-none"></div>
    <div aria-hidden="true" class="absolute top-1/3 right-0 w-72 h-72 bg-blue-500/[0.04] blur-3xl pointer-events-none"></div>

    <div class="relative max-w-3xl mx-auto text-center">
      <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full border border-accent/30 bg-accent/5 text-accent text-xs font-mono tracking-wider mb-8">
        <span class="w-1.5 h-1.5 rounded-full bg-accent animate-pulse"></span>
        KNOT · PEER-TO-PEER VERSION CONTROL
      </div>

      <h1 class="text-4xl md:text-6xl font-black tracking-tight leading-[1.05] mb-6">
        <span class="text-primary">Your code.</span><br/>
        <span class="text-gradient">Your machines.</span><br/>
        <span class="text-primary">No middleman.</span>
      </h1>

      <p class="text-lg text-secondary font-light max-w-xl mx-auto leading-relaxed mb-3">
        Take any folder on your computer. Knot keeps it backed up and in sync — in real time — across your own machines and your teammates'. No central server, no cloud account, no third party in between.
      </p>
      <p class="text-sm text-hint font-mono mb-2">
        Save → commit → synced. No add, no commit -m, no push.
      </p>
      <p class="text-sm text-accent font-mono mb-10">
        Even your AI coding assistants on different machines can build one project together over your network — hands-free, while you watch.
      </p>

      <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
        <a
          href="#early-access"
          class="px-8 py-3.5 rounded-xl bg-accent text-bg font-bold text-sm hover:bg-white transition-all hover:scale-105 hover:shadow-[0_0_32px_rgba(0,212,170,0.4)]"
        >
          Request early access
        </a>
        <a
          href="#architecture"
          class="px-8 py-3.5 rounded-xl border border-border text-secondary text-sm hover:border-accent hover:text-primary transition-all"
        >
          See the architecture
        </a>
      </div>

      <!-- Pre-alpha honesty pill — sits below CTAs so it doesn't dominate
           the hero, but is visible above the fold for anyone scrolling
           the page deciding whether to engage. -->
      <div class="mt-10 inline-flex items-center gap-2 px-3 py-1.5 rounded-full border border-yellow-500/30 bg-yellow-500/5 text-yellow-500/90 text-xs font-mono">
        <span class="w-1.5 h-1.5 rounded-full bg-yellow-500/80"></span>
        IN TESTING · pre-alpha · request access to try it
      </div>
    </div>
  </section>

  <!-- ── THE DESKTOP APP (UI showcase — desktop-window carousel) ───────────── -->
  <!-- Mirrors Heart's phone-mockup gallery, but in a desktop window frame
       because Knot is a desktop app. Intentionally un-numbered so it doesn't
       renumber the 01–08 chapter sequence below. -->
  <section id="screens" class="relative pb-24 px-6 overflow-hidden">
    <div aria-hidden="true" class="absolute top-1/4 left-1/2 -translate-x-1/2 w-[70%] h-72 bg-accent/[0.06] blur-3xl pointer-events-none"></div>

    <div class="relative max-w-5xl mx-auto">
      <div class="section-reveal text-center mb-10">
        <p class="text-accent text-xs font-mono tracking-widest mb-3">THE DESKTOP APP</p>
        <h2 class="text-2xl md:text-4xl font-light text-primary tracking-tight mb-3">Everything in one window.</h2>
        <p class="text-secondary text-sm max-w-xl mx-auto leading-relaxed">
          A live dashboard, built-in chat, AI-agent activity across your machines, and on-device settings. Knot is far more than “save = commit” — and it runs native on Linux, macOS, and Windows.
        </p>
      </div>

      <div id="knot-carousel" class="section-reveal flex flex-col items-center select-none">
        <!-- desktop window frame -->
        <div class="w-full rounded-xl border border-border bg-[#0e0e11] overflow-hidden"
             style="box-shadow:0 32px 80px rgba(0,0,0,0.6),0 0 0 1px rgba(255,255,255,0.025)">
          <!-- window chrome -->
          <div class="flex items-center gap-2 px-4 py-3 border-b border-border bg-surface">
            <span class="w-3 h-3 rounded-full bg-error/70"></span>
            <span class="w-3 h-3 rounded-full bg-yellow-500/70"></span>
            <span class="w-3 h-3 rounded-full bg-accent/70"></span>
            <span class="ml-3 text-xs font-mono text-hint">Knot — peer-to-peer version control</span>
          </div>
          <!-- screen -->
          <div class="relative" style="aspect-ratio:16/10">
            {screenshots.map((s, i) => (
              <img
                src={s.src}
                alt={s.alt}
                loading={i === 0 ? 'eager' : 'lazy'}
                class="absolute inset-0 w-full h-full object-cover object-top"
                style={`opacity:${i === 0 ? 1 : 0};z-index:${i === 0 ? 1 : 0};transition:opacity 0.45s ease`}
                data-knot-slide={i}
              />
            ))}
          </div>
        </div>

        <!-- caption -->
        <div class="relative mt-6 w-full" style="height:32px">
          {screenshots.map((s, i) => (
            <p
              class="absolute inset-0 flex items-center justify-center text-xs font-mono text-hint text-center px-4"
              style={`opacity:${i === 0 ? 1 : 0};transition:opacity 0.35s ease`}
              data-knot-caption={i}
            >
              {s.caption}
            </p>
          ))}
        </div>

        <!-- dots -->
        <div class="flex gap-3 mt-4">
          {screenshots.map((_, i) => (
            <button
              aria-label={`Screen ${i + 1} of ${screenshots.length}`}
              data-knot-dot={i}
              style={i === 0
                ? 'width:8px;height:8px;border-radius:50%;background:#00d4aa;transform:scale(1.3);transition:all 0.3s ease;border:none;cursor:pointer;padding:0'
                : 'width:8px;height:8px;border-radius:50%;background:#737373;transform:scale(1);transition:all 0.3s ease;border:none;cursor:pointer;padding:0'}
            />
          ))}
        </div>
      </div>
    </div>

    <script is:inline>
    (function () {
      var slides   = document.querySelectorAll('[data-knot-slide]');
      var captions = document.querySelectorAll('[data-knot-caption]');
      var dots     = document.querySelectorAll('[data-knot-dot]');
      if (!slides.length) return;
      var n = slides.length, cur = 0, timer = null;
      var reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      function show(i) {
        [slides, captions].forEach(function (col) {
          col[cur].style.opacity = '0'; col[cur].style.zIndex = '0';
          col[i].style.opacity   = '1'; col[i].style.zIndex   = '1';
        });
        dots[cur].style.background = '#737373'; dots[cur].style.transform = 'scale(1)';
        dots[i].style.background   = '#00d4aa'; dots[i].style.transform   = 'scale(1.3)';
        cur = i;
      }
      function advance() { show((cur + 1) % n); }
      function start() { if (reduced || timer) return; timer = setInterval(advance, 4500); }
      function stop()  { clearInterval(timer); timer = null; }
      dots.forEach(function (dot, i) { dot.addEventListener('click', function () { stop(); show(i); start(); }); });
      var c = document.getElementById('knot-carousel');
      c.addEventListener('mouseenter', stop);
      c.addEventListener('mouseleave', start);
      start();
    })();
    </script>
  </section>

  <!-- ── 01 THE PROBLEM ───────────────────────────────────────────────────── -->
  <section id="problem" class="relative py-28 px-6 max-w-4xl mx-auto">
    <div aria-hidden="true" class="absolute top-0 left-1/4 w-[40%] h-72 bg-accent/[0.06] blur-3xl pointer-events-none"></div>

    <div class="section-reveal text-center mb-16">
      <SectionEyebrow num="01" label="THE PROBLEM" />
      <h2 class="text-3xl md:text-5xl font-black text-primary mb-4">Your code lives on someone else's computer.</h2>
      <p class="text-secondary max-w-2xl mx-auto leading-relaxed">
        The usual way to share code puts it on a company's servers — push it up, pull it down. Your source then lives on infrastructure you don't control: someone else's access rules, uptime, and terms. Knot takes the middle out entirely — your code stays on your machines and moves directly between them.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
      <div class="p-6 rounded-2xl bg-surface border border-border">
        <p class="text-accent text-xs font-mono tracking-widest mb-2">FOR REGULATED TEAMS</p>
        <h3 class="text-lg font-bold text-primary mb-2">Code that legally can't leave the network</h3>
        <p class="text-secondary text-sm leading-relaxed">
          Defence, healthcare, finance — anything under data-residency rules. A hosted service is off the table, and running your own is an IT operational tax. Knot needs neither.
        </p>
      </div>
      <div class="p-6 rounded-2xl bg-surface border border-border">
        <p class="text-accent text-xs font-mono tracking-widest mb-2">FOR SMALL TEAMS</p>
        <h3 class="text-lg font-bold text-primary mb-2">A per-seat bill just to collaborate</h3>
        <p class="text-secondary text-sm leading-relaxed">
          A hosted service charges for every head, and the SSO / access-rules / CI setup is its own tax on shipping — for a team that only wants to share code.
        </p>
      </div>
      <div class="p-6 rounded-2xl bg-surface border border-border">
        <p class="text-accent text-xs font-mono tracking-widest mb-2">FOR SOLO DEVS</p>
        <h3 class="text-lg font-bold text-primary mb-2">The four-step push ritual</h3>
        <p class="text-secondary text-sm leading-relaxed">
          <code class="text-primary/70">git add</code> · <code class="text-primary/70">git commit -m</code> · <code class="text-primary/70">git push</code> · check teammate pulled. Four extra steps between thinking and shipping.
        </p>
      </div>
      <div class="p-6 rounded-2xl bg-surface border border-border">
        <p class="text-accent text-xs font-mono tracking-widest mb-2">FOR EVERYONE</p>
        <h3 class="text-lg font-bold text-primary mb-2">Availability on someone else's terms</h3>
        <p class="text-secondary text-sm leading-relaxed">
          Your access to your own code rides on a third party staying up, keeping your account, and serving your region. Knot's fix is structural: there's no central server in the middle at all. Your code lives on your own machines — your availability is your own.
        </p>
      </div>
    </div>
  </section>

  <!-- ── 02 STORYBOARD ────────────────────────────────────────────────────── -->
  <!-- Real-world scenario explaining Knot scene by scene. Every beat maps to
       a shipped capability; the killer AI-agent-collaboration beat (11:00) and
       the no-third-party-dependency beat (16:00) are the load-bearing ones. -->
  <section id="story" class="relative py-28 px-6 max-w-3xl mx-auto">
    <div aria-hidden="true" class="absolute top-0 right-1/4 w-[45%] h-72 bg-accent/[0.07] blur-3xl pointer-events-none"></div>

    <div class="section-reveal text-center mb-16">
      <SectionEyebrow num="02" label="STORYBOARD" />
      <h2 class="text-3xl md:text-5xl font-black text-primary mb-4">A day on a two-person team.</h2>
      <p class="text-secondary max-w-xl mx-auto leading-relaxed">
        Roshan builds on a Mac, Ammu on Linux — same Wi-Fi, nothing in between them. Here's an ordinary Tuesday. Two people here, but the same loop runs across as many machines — and as many AI agents — as you pair: a five-person startup or a two-thousand-engineer org, same flow.
      </p>
    </div>

    <div class="space-y-4">
      {[
        { t: '09:00', h: 'No setup. No accounts.', b: 'Roshan drops the project folder into Knot on the Mac. Ammu does the same on the Linux laptop. One pairing code, scanned once — that is the whole setup. No accounts, no SSH keys, no server to provision.' },
        { t: '09:15', h: 'One saves. The other already has it.', b: 'Roshan fixes a bug and hits save. A few seconds later it is a real commit on Ammu\'s machine — no add, no commit, no push. Ammu keeps building on top of it without lifting a finger.' },
        { t: '11:00', h: 'AI agents. One repo. Built together.', b: 'Roshan\'s coding agent on the Mac and Ammu\'s agent on the Linux box split the work — backend on one, frontend on the other — and build the feature together over the office Wi-Fi. Add more machines and more agents and they coordinate across all of them. No human in the loop, and you watch every file they write and every command they run, live, on screen.' },
        { t: '11:30', h: 'Guardrails that actually hold.', b: 'Ammu scoped the agent to the app source only. When it reaches toward the secrets file, Knot blocks the commit and records the attempt. The secret never moves.' },
        { t: '13:00', h: 'Proof, on demand.', b: 'Someone asks who changed the auth flow. Roshan exports a signed record — every step attributed to a person or an agent session, tamper-evident, and verifiable by anyone. No server was asked.' },
        { t: '16:00', h: 'Nobody else\'s uptime. Nobody else\'s rules.', b: 'No central server ever sat between Roshan and Ammu — so there is no account to lock, no region to block, no seat to pay for, and no provider whose uptime they live or die by. The repo is on their machines; their availability is their own.' },
        { t: 'evening', h: 'Check it from the couch.', b: 'Roshan opens the project on an iPad on the same Wi-Fi — a live, read-only view of the repo, with every new commit appearing as it lands. No app store, no account.' },
      ].map(({ t, h, b }) => (
        <div class="section-reveal flex gap-5 p-5 rounded-2xl bg-surface border border-border hover:border-accent/30 transition-colors">
          <div class="shrink-0 w-16 text-accent text-xs font-mono tracking-widest pt-1">{t}</div>
          <div>
            <h3 class="text-base font-bold text-primary mb-1.5 leading-snug">{h}</h3>
            <p class="text-secondary text-sm leading-relaxed">{b}</p>
          </div>
        </div>
      ))}
    </div>

    <p class="section-reveal text-center text-hint text-sm mt-10 max-w-xl mx-auto">
      No server appears anywhere in that day — because there isn't one. That's the whole idea.
    </p>
  </section>

  <!-- ── 03 HOW IT WORKS ──────────────────────────────────────────────────── -->
  <section id="how" class="relative py-28 px-6 bg-surface border-y border-border overflow-hidden">
    <div aria-hidden="true" class="absolute top-0 left-1/2 -translate-x-1/2 w-[80%] h-72 bg-accent/[0.08] blur-3xl pointer-events-none"></div>
    <div class="relative max-w-4xl mx-auto">
      <div class="section-reveal text-center mb-16">
        <SectionEyebrow num="03" label="HOW IT WORKS" />
        <h2 class="text-3xl md:text-5xl font-black text-primary mb-4">Three pieces. You only see one.</h2>
        <p class="text-secondary max-w-xl mx-auto leading-relaxed">
          The other two — a small background helper and the encrypted Tor connection — work on their own and stay invisible.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {[
          {
            step: '01',
            title: 'The folder',
            body: 'Pick any folder on your machine. Tell Knot once. It now behaves like a git repository — because under the hood it is one. Existing `git` commands still work.',
          },
          {
            step: '02',
            title: 'The peers',
            body: 'Anyone you want to collaborate with installs Knot. You give them a one-time pairing code; they accept it. From that moment, your two machines are paired.',
          },
          {
            step: '03',
            title: 'The save',
            body: 'Open the file. Write code. Hit save. Knot notices the change, waits a moment for you to finish, makes a real git commit, and sends it to every paired teammate. They see it within seconds.',
          },
        ].map(({ step, title, body }) => (
          <div class="section-reveal relative pl-8 border-l border-border hover:border-accent/40 transition-colors">
            <span class="absolute -left-px top-0 w-0.5 h-8 bg-accent"></span>
            <p class="text-accent font-mono text-xs mb-2">{step}</p>
            <h4 class="text-lg font-bold text-primary mb-2">{title}</h4>
            <p class="text-secondary text-sm leading-relaxed">{body}</p>
          </div>
        ))}
      </div>

      <p class="text-center text-hint text-xs mt-12 max-w-xl mx-auto leading-relaxed">
        Same wire-format efficiency as `git fetch` (real pack files). Same content-addressing guarantees (SHA-256 hash chain). What changes is who owns the bytes in transit — only you and your peers.
      </p>
    </div>
  </section>

  <!-- ── 03 FEATURES ──────────────────────────────────────────────────────── -->
  <section id="features" class="relative py-28 px-6 max-w-7xl mx-auto">
    <div aria-hidden="true" class="absolute top-0 right-0 w-[40%] h-72 bg-accent/[0.06] blur-3xl pointer-events-none"></div>

    <div class="section-reveal text-center mb-16">
      <SectionEyebrow num="04" label="FEATURES" />
      <h2 class="text-3xl md:text-5xl font-black text-primary mb-4">What it does</h2>
      <p class="text-secondary max-w-xl mx-auto leading-relaxed">
        Everything below is built and verified end-to-end. The full v1 milestone passes on real hardware across networks.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-5">
      <FeatureBlock
        icon="🤝"
        title="AI agents build together"
        description="AI assistants on your paired machines build one project between them — hands-free, no human in the loop, and you watch every step live. The thing a central host can't do."
      />
      <FeatureBlock
        icon="💾"
        title="Saves are commits"
        description="Knot turns every save into a real git commit a moment after you stop typing — no `git add`, no `commit -m`, no `push`."
      />
      <FeatureBlock
        icon="📡"
        title="Reaches peers on any network"
        description="Commits travel to every teammate over Tor, encrypted end-to-end — on home Wi-Fi, hotspots, even locked-down office networks. No ports to open."
      />
      <FeatureBlock
        icon="🪪"
        title="Pairing codes, not accounts"
        description="One code carries everything the other device needs — paste it or scan the QR. Zero accounts, zero email."
      />
      <FeatureBlock
        icon="🪢"
        title="Real git underneath"
        description="Knot uses git's real object format, so `git log`, `git diff`, and `git branch` all still work underneath — nothing to relearn."
      />
      <FeatureBlock
        icon="🧾"
        title="Provable, governed AI"
        description="Every commit is signed by the person and the AI session behind it, and each agent is scoped to the paths it may touch. Out-of-scope changes never commit; export a tamper-evident record anyone can verify."
      />
    </div>
  </section>

  <!-- ── 04 ARCHITECTURE ──────────────────────────────────────────────────── -->
  <section id="architecture" class="relative py-32 px-6 bg-surface border-y border-border overflow-hidden">
    <div aria-hidden="true" class="absolute top-0 left-1/2 -translate-x-1/2 w-[80%] h-72 bg-accent/[0.08] blur-3xl pointer-events-none"></div>
    <div class="relative max-w-4xl mx-auto">
      <div class="section-reveal text-center mb-12">
        <SectionEyebrow num="05" label="ARCHITECTURE" />
        <h2 class="text-3xl md:text-5xl font-light text-primary mb-4 tracking-tight">Receipts, not claims.</h2>
        <p class="text-secondary max-w-xl mx-auto">
          The cryptographic and transport primitives Knot ships are open and inspectable. Pure-Rust, single binary, zero system services.
        </p>
      </div>

      <!-- ── Visual end-to-end diagram ──────────────────────────────────────
           The conceptual map of "save → commit → propagate" for readers
           who don't read terminal panels. The technical reference below
           is the same content in primitive-by-primitive form. Both are
           load-bearing — don't drop either. (Pattern mirrored from the
           Whispr product page.) -->
      <div class="section-reveal mb-12">
        <KnotArchitectureDiagram />
      </div>

      <div class="section-reveal font-mono text-sm rounded-2xl bg-bg border border-border overflow-hidden">
        <div class="flex items-center gap-2 px-5 py-3 border-b border-border bg-surface">
          <div class="w-3 h-3 rounded-full bg-error/70"></div>
          <div class="w-3 h-3 rounded-full bg-yellow-500/70"></div>
          <div class="w-3 h-3 rounded-full bg-accent/70"></div>
          <span class="ml-2 text-hint text-xs">knot-architecture.txt</span>
        </div>
        <div class="p-6 space-y-2.5 text-xs md:text-sm leading-relaxed overflow-x-auto">
          {[
            ['Identity',        'Ed25519 keypair · generated on-device · stored at OS-permission 0600'],
            ['Pairing',         'Mutual `knot1:<base64>` blob exchange · explicit confirm on both sides'],
            ['Wire auth',       'Ed25519 challenge–response · per-connection nonces · no replay'],
            ['Transport',       'Arti (pure-Rust Tor) · onion-v3 hidden services · no clearnet bytes'],
            ['Object format',   'Real git pack files · libgit2 · `git` CLI works on a Knot folder'],
            ['Push',            'Bundle over Tor · fast-forward only · non-FF lands in refs/knot-incoming/'],
            ['Capabilities',    'Ed25519-signed grants · offline-verifiable · instant revocation'],
            ['Coordinator',     'Optional · sees encrypted bytes only · cannot tamper with the hash chain'],
            ['Server',          'None'],
          ].map(([key, val]) => (
            <div class="flex gap-4 items-baseline">
              <span class="text-hint shrink-0 w-32">{key}</span>
              <span class="text-accent">→</span>
              <span class="text-primary/80">{val}</span>
            </div>
          ))}
        </div>
      </div>

      <p class="text-hint text-xs text-center mt-6 max-w-xl mx-auto leading-relaxed">
        Same Tor primitives that route Whispr's messages and protect journalists worldwide. Same git object format that powers every git host on the internet. We didn't reinvent the cryptography — we removed the centralised server.
      </p>
    </div>
  </section>

  <!-- ── 06 PROOF ─────────────────────────────────────────────────────────── -->
  <!-- Token-efficiency benchmark + A/B. Honest framing per the corrected
       evidence doc: the A/B (same task, real counts) is the headline; the
       raw-size ratios are shown as a labelled CEILING, never as the everyday
       saving. Every number here is reproducible by the reader. -->
  <section id="proof" class="relative py-32 px-6 bg-surface border-y border-border overflow-hidden">
    <div aria-hidden="true" class="absolute top-0 right-1/4 w-[45%] h-72 bg-accent/[0.08] blur-3xl pointer-events-none"></div>
    <div class="relative max-w-4xl mx-auto">
      <div class="section-reveal text-center mb-14">
        <SectionEyebrow num="06" label="PROOF" />
        <h2 class="text-3xl md:text-5xl font-black text-primary mb-4">Measured, not claimed.</h2>
        <p class="text-secondary max-w-2xl mx-auto leading-relaxed">
          Knot's headline benefit for AI agents: one reads a repository's whole shape for a few thousand tokens — instead of burning tens of thousands listing folders and opening files to find its bearings. That's a testable claim, so we tested it. Every number below is reproducible on your own machine.
        </p>
      </div>

      <!-- Benchmark 1 — the A/B (the defensible centrepiece). -->
      <div class="section-reveal mb-6">
        <p class="text-accent text-xs font-mono tracking-widest mb-3 text-center">BENCHMARK 1 · SAME REPO · SAME QUESTION · SAME AGENT</p>
        <p class="text-secondary text-sm text-center max-w-2xl mx-auto mb-8">
          A 56-file Python codebase the agent had never read. The question, both times:
          <span class="text-primary/90">“What is this project, what are its main components, and where would a new forecasting model go?”</span> Both answers were correct against the source. Only the cost differed.
        </p>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
          <div class="p-6 rounded-2xl bg-bg border border-border">
            <p class="text-hint text-xs font-mono tracking-widest mb-3">WITHOUT KNOT</p>
            <p class="text-4xl font-black text-primary mb-1">≈ 5,185<span class="text-lg text-hint font-bold"> tokens</span></p>
            <p class="text-hint text-xs font-mono mb-4">5+ tool calls</p>
            <ul class="text-secondary text-sm space-y-1.5 leading-relaxed">
              <li>list the files · read the README</li>
              <li>open <code class="text-primary/70">base.py</code> · <code class="text-primary/70">registry.py</code> · a model</li>
              <li class="text-hint">…and that's granting perfect first-try file picks</li>
            </ul>
          </div>
          <div class="p-6 rounded-2xl bg-accent/[0.06] border border-accent/40">
            <p class="text-accent text-xs font-mono tracking-widest mb-3">WITH KNOT</p>
            <p class="text-4xl font-black text-accent mb-1">≈ 1,504<span class="text-lg text-accent/70 font-bold"> tokens</span></p>
            <p class="text-accent/70 text-xs font-mono mb-4">1 tool call</p>
            <ul class="text-secondary text-sm space-y-1.5 leading-relaxed">
              <li>one call to <code class="text-primary/80">knot_repo_map</code></li>
              <li>files ranked by importance, each with its symbols</li>
              <li class="text-primary/80">complete, correct answer</li>
            </ul>
          </div>
        </div>
        <p class="text-secondary text-sm text-center mt-6 max-w-2xl mx-auto leading-relaxed">
          <span class="text-primary font-semibold">≥ 3.4× fewer tokens — on a small repo, with every assumption biased against Knot.</span> The map is budget-capped (~2,000 tokens whether the repo has 56 files or 5,000), while ad-hoc exploration scales with the codebase — so the gap widens as projects grow.
        </p>
      </div>

      <!-- Benchmark 2 — the raw-size ratios, explicitly labelled as a ceiling. -->
      <div class="section-reveal mt-14 font-mono text-sm rounded-2xl bg-bg border border-border overflow-hidden">
        <div class="flex items-center gap-2 px-5 py-3 border-b border-border bg-surface">
          <div class="w-3 h-3 rounded-full bg-error/70"></div>
          <div class="w-3 h-3 rounded-full bg-yellow-500/70"></div>
          <div class="w-3 h-3 rounded-full bg-accent/70"></div>
          <span class="ml-2 text-hint text-xs">token_bench.sh · two real repositories</span>
        </div>
        <div class="p-6 space-y-3 text-xs md:text-sm leading-relaxed overflow-x-auto">
          <div class="flex gap-4 items-baseline text-hint">
            <span class="shrink-0 w-44">repository</span>
            <span class="shrink-0 w-24 text-right">raw source</span>
            <span class="shrink-0 w-24 text-right">knot map</span>
            <span class="text-right">ratio</span>
          </div>
          {[
            ['Heart · Flutter + Python · 226 files', '≈390,000', '≈2,006', '194 : 1'],
            ['Knot · Rust workspace · 106 files',    '≈652,000', '≈2,005', '325 : 1'],
          ].map(([repo, raw, map, ratio]) => (
            <div class="flex gap-4 items-baseline">
              <span class="text-primary/80 shrink-0 w-44">{repo}</span>
              <span class="text-hint shrink-0 w-24 text-right">{raw}</span>
              <span class="text-accent shrink-0 w-24 text-right">{map}</span>
              <span class="text-primary font-semibold text-right flex-1">{ratio}</span>
            </div>
          ))}
          <div class="pt-3 text-hint text-[11px] leading-relaxed border-t border-border/60 mt-3">
            tokens ≈ bytes ÷ 4 · raw counts source only, with vendored/build dirs already excluded
          </div>
        </div>
      </div>
      <p class="text-hint text-xs text-center mt-4 max-w-2xl mx-auto leading-relaxed">
        This ratio is a <span class="text-secondary">ceiling</span>, not the everyday saving — no agent reads an entire repo. It bounds the problem; Benchmark 1 is the realistic number.
      </p>

      <!-- Verify it yourself — the third-party-checkable protocol. -->
      <div class="section-reveal mt-12 p-6 rounded-2xl bg-bg border border-border max-w-2xl mx-auto">
        <p class="text-accent text-xs font-mono tracking-widest mb-3">VERIFY IT YOURSELF</p>
        <p class="text-secondary text-sm leading-relaxed mb-3">
          Don't take our number — generate your own. Run two fresh AI-agent sessions on the same repository: one with Knot's tools available, one without. Ask each the same orientation question, then compare the token cost the agent itself reports.
        </p>
        <p class="text-hint text-xs leading-relaxed">
          Same task, same model, same repo — metered by the agent's own billing, not by us. The bundled <code class="text-primary/70">knot</code> CLI also ships a one-screen benchmark script you can point at any folder.
        </p>
      </div>

      <p class="text-hint text-xs text-center mt-6 max-w-xl mx-auto leading-relaxed">
        Honest limits: the map doesn't replace reading code you're about to <em>edit</em> — it replaces the hunt for <em>which</em> code that is. Per-file symbol extraction currently covers Rust, Python, JavaScript/TypeScript, and Dart.
      </p>
    </div>
  </section>

  <!-- ── KNOT AI (on-device commit-message model) ─────────────────────────────
       Intentionally un-numbered — like THE DESKTOP APP showcase — so it doesn't
       renumber the 01–10 chapter sequence. Knot AI is a capability of Knot (the
       on-device model that writes commit messages), now published openly on
       Hugging Face + Ollama. Benchmark rendered as native themed CSS bars/table
       (NOT the white PNGs used on HF/Ollama) so it matches the dark palette.
       Every number here comes from the same 3-model x 2-gold-set eval. -->
  <section id="knot-ai" class="relative py-32 px-6 overflow-hidden">
    <div aria-hidden="true" class="absolute top-0 left-1/2 -translate-x-1/2 w-[70%] h-72 bg-accent/[0.07] blur-3xl pointer-events-none"></div>
    <div class="relative max-w-4xl mx-auto">
      <div class="section-reveal text-center mb-14">
        <div class="flex items-center justify-center gap-3 mb-4">
          <img src="/knot-ai-logo.png" alt="Knot AI" width="52" height="52" class="rounded-xl" />
          <p class="text-accent text-xs font-mono tracking-widest">KNOT AI · ON-DEVICE MODEL</p>
        </div>
        <h2 class="text-3xl md:text-5xl font-black text-primary mb-4">The model that writes your commits.</h2>
        <p class="text-secondary max-w-2xl mx-auto leading-relaxed">
          When a save becomes a commit, something has to write the message. Knot AI does — a task-specific model that reads your diff and writes a clear, correctly-typed Conventional-Commit message. It runs <span class="text-primary">100% on your machine</span>: no API, no code leaving your device. It's a fine-tune of Qwen2.5-Coder-7B, and it's <span class="text-primary">open</span> — on Hugging Face and Ollama.
        </p>
      </div>

      <!-- How it works — one mono line, same visual grammar as the hero coda. -->
      <div class="section-reveal text-center mb-14">
        <p class="inline-flex flex-wrap items-center justify-center gap-x-3 gap-y-1 text-xs font-mono text-hint">
          <span class="text-primary/80">git diff</span>
          <span class="text-accent">→</span>
          <span class="text-primary/80">Qwen2.5-Coder-7B + Knot AI LoRA</span>
          <span class="text-accent">→</span>
          <span class="text-primary/80">fix: prevent division by zero</span>
        </p>
      </div>

      <!-- Benchmark: measured, not claimed — matches the PROOF section's stance. -->
      <div class="section-reveal">
        <p class="text-accent text-xs font-mono tracking-widest mb-3 text-center">BENCHMARK · 60 HELD-OUT COMMITS · SCORED THROUGH THE SHIPPING MODEL</p>
        <p class="text-secondary text-sm text-center max-w-2xl mx-auto mb-10">
          Three models under one identical harness: the untouched base, the prior release (v3, JS/TS only), and today's model (v4, with added Rust / Go / Python). The metric is whether it picks the right commit <em>type</em> — the hardest, most useful signal.
        </p>

        <!-- Fine-tuning ladder — the headline chart, native CSS bars. -->
        <div class="rounded-2xl bg-bg border border-border p-6 md:p-8 mb-6">
          <p class="text-hint text-xs font-mono tracking-widest mb-6">TYPE ACCURACY — THE FINE-TUNING LADDER</p>
          {[
            { name: 'Base Qwen (zero-shot)', val: 0,  pct: '0%',   fill: 'bg-hint/40',   txt: 'text-hint',   note: '59/60 replies unparseable' },
            { name: 'Knot AI v3 (prior)',    val: 40, pct: '40%',  fill: 'bg-accent/45', txt: 'text-secondary', note: 'JS/TS only' },
            { name: 'Knot AI (v4)',          val: 55, pct: '55%',  fill: 'bg-accent',    txt: 'text-accent',    note: '0/60 errors' },
          ].map(({ name, val, pct, fill, txt, note }) => (
            <div class="flex items-center gap-3 md:gap-4 mb-4 last:mb-0">
              <span class="w-32 md:w-44 shrink-0 text-[11px] md:text-xs font-mono text-secondary text-right leading-tight">{name}</span>
              <div class="flex-1 h-7 rounded-md bg-surface border border-border overflow-hidden">
                <div class={`h-full ${fill} rounded-md transition-all`} style={`width:${val}%`}></div>
              </div>
              <span class={`w-10 shrink-0 text-xs font-mono ${txt}`}>{pct}</span>
              <span class="hidden md:block w-40 shrink-0 text-[11px] font-mono text-hint leading-tight">{note}</span>
            </div>
          ))}
        </div>

        <!-- Full metrics table (JS/TS gold). -->
        <div class="section-reveal rounded-2xl bg-bg border border-border overflow-hidden mb-6">
          <div class="flex items-center gap-2 px-5 py-3 border-b border-border bg-surface">
            <div class="w-3 h-3 rounded-full bg-error/70"></div>
            <div class="w-3 h-3 rounded-full bg-yellow-500/70"></div>
            <div class="w-3 h-3 rounded-full bg-accent/70"></div>
            <span class="ml-2 text-hint text-xs font-mono">knot-ai-benchmark.txt · 60 JS/TS gold</span>
          </div>
          <div class="overflow-x-auto">
            <table class="w-full text-xs md:text-sm font-mono">
              <thead>
                <tr class="text-hint border-b border-border">
                  <th class="text-left font-normal p-3 md:px-5">Metric</th>
                  <th class="text-right font-normal p-3 md:px-5">Base</th>
                  <th class="text-right font-normal p-3 md:px-5">v3</th>
                  <th class="text-right font-semibold p-3 md:px-5 text-accent">Knot AI</th>
                </tr>
              </thead>
              <tbody>
                {[
                  ['Type accuracy',        '0.0%', '40.0%', '55.0%'],
                  ['Title word-overlap',   '0.4%', '18.3%', '30.3%'],
                  ['Exact title',          '0.0%', '0.0%',  '6.7%'],
                  ['Scope accuracy',       '0.0%', '70.0%', '75.0%'],
                  ['Unparseable / 60',     '59',   '4',     '0'],
                ].map(([metric, base, v3, knot]) => (
                  <tr class="border-b border-border/50 last:border-0">
                    <td class="text-secondary p-3 md:px-5">{metric}</td>
                    <td class="text-hint text-right p-3 md:px-5">{base}</td>
                    <td class="text-secondary text-right p-3 md:px-5">{v3}</td>
                    <td class="text-accent text-right font-semibold p-3 md:px-5">{knot}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>

        <!-- Cross-language generalization — separate gold set. -->
        <div class="section-reveal rounded-2xl bg-bg border border-border p-6 md:p-8 mb-8">
          <p class="text-hint text-xs font-mono tracking-widest mb-2">GENERALIZES ACROSS LANGUAGES</p>
          <p class="text-secondary text-sm mb-6 leading-relaxed">
            Type accuracy on a <span class="text-primary/80">separate</span> 60-commit set of Rust / Go / Python — languages this release added. The base model scores 0% on all three.
          </p>
          {[
            { lang: 'Rust',   val: 55 },
            { lang: 'Go',     val: 45 },
            { lang: 'Python', val: 60 },
          ].map(({ lang, val }) => (
            <div class="flex items-center gap-3 md:gap-4 mb-3.5 last:mb-0">
              <span class="w-16 md:w-20 shrink-0 text-xs font-mono text-secondary text-right">{lang}</span>
              <div class="flex-1 h-6 rounded-md bg-surface border border-border overflow-hidden">
                <div class="h-full bg-accent rounded-md" style={`width:${val}%`}></div>
              </div>
              <span class="w-10 shrink-0 text-xs font-mono text-accent">{val}%</span>
            </div>
          ))}
        </div>

        <!-- Honest read — same credibility-by-honesty pattern as the rest of the page. -->
        <p class="section-reveal text-hint text-xs text-center max-w-2xl mx-auto leading-relaxed mb-10">
          Honest read: strong on the high-signal types (chore, ci, feat, fix, test), still developing on the genuinely ambiguous ones — refactor vs feat, build vs chore — which the next release targets. These are single-reference scores (compared to one human label), so they understate real usefulness: a change scored a "miss" for not matching the exact human wording is still a correct, useful message.
        </p>

        <!-- Published links — the model is public; source stays private. -->
        <div class="section-reveal flex flex-col sm:flex-row items-center justify-center gap-4">
          <a
            href="https://huggingface.co/pointbreaklab/knot-ai"
            target="_blank" rel="noopener"
            class="inline-flex items-center gap-2 px-6 py-3 rounded-xl border border-border text-secondary text-sm font-mono hover:border-accent hover:text-primary transition-all"
          >
            🤗 Hugging Face
            <span class="text-hint">pointbreaklab/knot-ai</span>
          </a>
          <a
            href="https://ollama.com/pointbreaklab/knot-ai"
            target="_blank" rel="noopener"
            class="inline-flex items-center gap-2 px-6 py-3 rounded-xl border border-border text-secondary text-sm font-mono hover:border-accent hover:text-primary transition-all"
          >
            🦙 Ollama
            <span class="text-hint">ollama run pointbreaklab/knot-ai</span>
          </a>
        </div>

        <p class="section-reveal text-hint text-[11px] text-center mt-8 max-w-xl mx-auto leading-relaxed">
          Base: Qwen2.5-Coder-7B-Instruct · Method: QLoRA (4-bit) on a single NVIDIA RTX 3060 · Trained only on permissively-licensed public commit history — no user data, no private code · License: Apache-2.0.
        </p>
      </div>
    </div>
  </section>

  <!-- ── 07 WHO IT'S FOR ──────────────────────────────────────────────────── -->
  <section id="audiences" class="relative py-32 px-6 max-w-6xl mx-auto">
    <div aria-hidden="true" class="absolute top-0 left-1/4 w-[40%] h-64 bg-accent/[0.05] blur-3xl pointer-events-none"></div>

    <div class="section-reveal text-center mb-16">
      <SectionEyebrow num="07" label="WHO IT'S FOR" />
      <h2 class="text-3xl md:text-5xl font-black text-primary mb-4">Three audiences, one tool.</h2>
      <p class="text-secondary max-w-xl mx-auto leading-relaxed">
        Knot scales from one person on one laptop to a regulated enterprise — without changing tools, because the architecture fits all three.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-5">
      <div class="p-6 rounded-2xl bg-surface border border-accent/30 glow-border">
        <div class="inline-block px-2 py-0.5 rounded bg-accent/10 text-accent text-xs font-mono mb-3">SOLO</div>
        <h3 class="text-lg font-bold text-primary mb-2">Personal projects</h3>
        <p class="text-secondary text-sm leading-relaxed mb-3">
          You want version history and cross-device sync — without a subscription for an unfinished side-project, and without uploading it to a company's servers.
        </p>
        <p class="text-hint text-xs italic">
          "I just want my code to exist on hardware I own and not be lost."
        </p>
      </div>
      <div class="p-6 rounded-2xl bg-surface border border-accent/30 glow-border">
        <div class="inline-block px-2 py-0.5 rounded bg-accent/10 text-accent text-xs font-mono mb-3">STARTUP</div>
        <h3 class="text-lg font-bold text-primary mb-2">Small distributed team</h3>
        <p class="text-secondary text-sm leading-relaxed mb-3">
          One-time install per laptop. Teammate added in 30 seconds with a pairing code. No org to administer, no per-seat bill that grows with every hire.
        </p>
        <p class="text-hint text-xs italic">
          "We don't want to stand up a hosted org just to share code with five people who already trust each other."
        </p>
      </div>
      <div class="p-6 rounded-2xl bg-surface border border-accent/30 glow-border">
        <div class="inline-block px-2 py-0.5 rounded bg-accent/10 text-accent text-xs font-mono mb-3">ENTERPRISE</div>
        <h3 class="text-lg font-bold text-primary mb-2">Centralised office</h3>
        <p class="text-secondary text-sm leading-relaxed mb-3">
          Code never crosses the firewall. Every commit signed by developer + AI session. Per-path scopes for what agents may touch. Compliance answer, audit answer, IT-relief answer — same architecture.
        </p>
        <p class="text-hint text-xs italic">
          "Legal won't let our code leave the network, but we still want our developers to use modern AI tools."
        </p>
      </div>
    </div>
  </section>

  <!-- ── 06 STATUS & ROADMAP ──────────────────────────────────────────────── -->
  <section id="roadmap" class="relative py-32 px-6 max-w-4xl mx-auto">
    <div aria-hidden="true" class="absolute top-0 right-0 w-[50%] h-64 bg-accent/[0.05] blur-3xl pointer-events-none"></div>
    <div class="section-reveal text-center mb-16">
      <SectionEyebrow num="08" label="STATUS & ROADMAP" />
      <h2 class="text-3xl md:text-5xl font-black text-primary mb-4">Where we are.</h2>
      <p class="text-secondary max-w-xl mx-auto leading-relaxed">
        Knot is in pre-alpha. Every storyline below has working code; some are still being hardened for general release.
      </p>
    </div>

    <div class="space-y-4">
      {[
        {
          tag: 'IN TESTING',
          title: 'v1 milestone — end-to-end on real hardware',
          body: 'Save → commit → sync across two machines on two different networks, entirely over Tor, in seconds. Verified Mac ↔ Linux on home Wi-Fi and a mobile hotspot.',
          status: 'building',
        },
        {
          tag: 'VERIFIED',
          title: 'Hands-free sync, no server',
          body: 'Every save fans out to each paired device automatically — no manual push. Want an always-on backup? Run a self-hosted coordinator or backup peer from one Docker command, on a Pi or a $5 VPS.',
          status: 'building',
        },
        {
          tag: 'VERIFIED',
          title: 'AI collaboration you can prove',
          body: 'AI assistants on different machines build one project together, watched live — every action signed and exportable as a tamper-evident audit trail, and each agent scoped to the paths it may touch.',
          status: 'building',
        },
        {
          tag: 'VERIFIED',
          title: 'Built for non-developers',
          body: 'Plain-language conflict resolution (keep mine / theirs / both — never raw git markers), a read-only mobile mirror over Wi-Fi, six interface languages, and a one-switch offline mode.',
          status: 'building',
        },
        {
          tag: 'IN PROGRESS',
          title: 'Onboarding + public installer',
          body: 'Autostart and one-click Solo mode are in. The full first-run wizard and a public, notarized installer (Apple Developer ID + Windows MSI) are the last gate before an open download lands here.',
          status: 'soon',
        },
        {
          tag: 'PLANNED',
          title: 'Merge of divergent histories',
          body: 'Single-file conflicts are handled today. Full three-way merge across diverged branches is still manual via `git` — which works, because it is a real git repo.',
          status: 'planned',
        },
      ].map(({ tag, title, body, status }) => {
        const cardBorder =
          status === 'building' ? 'border-accent/40 hover:border-accent/60 shadow-[0_0_24px_rgba(0,212,170,0.06)]'
          : status === 'soon'   ? 'border-yellow-500/30 hover:border-yellow-500/50'
          :                       'border-border hover:border-accent/20';
        const dot =
          status === 'building' ? 'bg-accent animate-pulse'
          : status === 'soon'   ? 'bg-yellow-500/70'
          :                       'bg-hint';
        const pill =
          status === 'building' ? 'bg-accent/10 text-accent border border-accent/30'
          : status === 'soon'   ? 'bg-yellow-500/10 text-yellow-500/90 border border-yellow-500/30'
          :                       'bg-surface text-hint border border-border';
        const label =
          status === 'building' ? 'building'
          : status === 'soon'   ? 'soon'
          :                       'planned';
        return (
          <div class={`section-reveal flex gap-5 p-6 rounded-2xl bg-surface border transition-colors ${cardBorder}`}>
            <div class={`mt-1 w-2 h-2 rounded-full shrink-0 ${dot}`}></div>
            <div>
              <p class="text-hint text-xs font-mono mb-1">{tag}</p>
              <h3 class="text-base font-bold text-primary mb-1.5">{title}</h3>
              <p class="text-secondary text-sm leading-relaxed">{body}</p>
            </div>
            <div class={`ml-auto shrink-0 self-start px-2 py-0.5 rounded text-xs font-mono ${pill}`}>
              {label}
            </div>
          </div>
        );
      })}
    </div>
  </section>

  <!-- ── 07 FAQ ───────────────────────────────────────────────────────────── -->
  <!-- Seven plain-English answers to the questions every new visitor asks
       within the first two minutes. Pattern: question as the bold lead,
       short paragraph answer, no jargon unless it's load-bearing. Order
       is the order people actually ask in. Don't reorder without thinking
       about that. -->
  <section id="faq" class="relative py-28 px-6 max-w-4xl mx-auto">
    <div aria-hidden="true" class="absolute top-0 left-1/4 w-[50%] h-72 bg-accent/[0.06] blur-3xl pointer-events-none"></div>

    <div class="section-reveal text-center mb-16">
      <SectionEyebrow num="09" label="FAQ" />
      <h2 class="text-3xl md:text-5xl font-black text-primary mb-4">The questions everyone asks.</h2>
      <p class="text-secondary max-w-xl mx-auto leading-relaxed">
        Plain-English answers. If something's still unclear, the contact form is at the bottom.
      </p>
    </div>

    <div class="space-y-4">
      {[
        {
          q: 'Who should use Knot?',
          a: 'Three groups, roughly: (1) solo developers who want version history and cross-device sync without a subscription or uploading unfinished work to someone else\'s servers; (2) small teams who don\'t want a per-seat bill and an org to administer just to share code with people they already trust; (3) enterprises where compliance or contracts mean code legally can\'t leave the network. The same Knot install fits all three — the architecture doesn\'t change when your team grows.',
        },
        {
          q: 'Is Knot safe?',
          a: 'Every connection uses Ed25519 mutual authentication — both sides cryptographically prove their identity before any byte of code crosses the wire. The wire itself is Tor (.onion hidden services), so the bytes are encrypted end-to-end and relays only ever see ciphertext. Pairing a new device requires explicit confirmation on both sides; trust never happens by accident. The same cryptographic primitives we use are the ones Signal, Tor, and OpenSSH ship — not new crypto, well-audited crypto.',
        },
        {
          q: 'Does Knot use a server?',
          a: 'No. Code transfer is always peer-to-peer; your machines talk directly to your teammates\' machines through Tor circuits. There is an optional small "coordinator" service that helps peers discover each other across the internet, but it never sees a single byte of your code — only "peer X says they host project Y." You can run it yourself on a $35 Raspberry Pi (one Docker command), use a community-hosted one, or skip it entirely if your peers are on the same network.',
        },
        {
          q: 'Do I still get to use `git`?',
          a: 'Yes. Knot uses git\'s real object format underneath, so a Knot-managed folder is also a real git repository. Every git command you already know — `git log`, `git diff`, `git branch`, `git checkout` — works on it. Knot is the layer above git that automates the save-commit-push ritual and removes the central server. It doesn\'t replace git; it makes git invisible when you don\'t need it.',
        },
        {
          q: 'What if my collaborator is offline when I save?',
          a: 'Nothing transfers until they come back online — same as any peer-to-peer protocol. Your commit lives safely on your machine; once your collaborator\'s daemon connects, the queued commits sync automatically. Common workaround: a third peer that\'s always online (a home server, a $5/month tiny VPS, a Raspberry Pi) holds a copy and bridges the gap. We ship that as a one-command Docker image — an always-on backup peer, reachable over Tor with no inbound ports to forward. It needs the same pairing as any other peer — it\'s not a "server," it\'s just a better-connected teammate.',
        },
        {
          q: 'What happens if I lose my keypair?',
          a: 'The same thing that happens if you lose your SSH key today: you generate a new keypair and your collaborators issue you a new pairing code. There is no central account to "recover" because there is no central account. The old key is dead; the new one is paired fresh. Your local repository is untouched — you can still read everything on your machine — you just have to re-establish trust with your peers, which takes about thirty seconds per peer.',
        },
        {
          q: 'Can I use Knot for things that aren\'t code?',
          a: 'Yes. Knot doesn\'t care what\'s in the folder — design assets, documents, datasets, screenplays, anything you want versioned and synced across machines. The save-becomes-commit loop works on any file the operating system can write. The one practical caveat is large binary files (videos, multi-gigabyte datasets): they\'ll work, but git\'s storage model is tuned for text, so very large binaries can bloat the history. Same caveat as git itself.',
        },
      ].map(({ q, a }) => (
        <details class="section-reveal group p-5 rounded-2xl bg-surface border border-border hover:border-accent/30 transition-colors">
          <summary class="cursor-pointer list-none flex items-start gap-4">
            <span class="text-accent text-xl font-mono shrink-0 transition-transform group-open:rotate-45">+</span>
            <span class="text-base font-bold text-primary leading-snug">{q}</span>
          </summary>
          <p class="text-secondary text-sm leading-relaxed mt-3 pl-9">
            {a}
          </p>
        </details>
      ))}
    </div>
  </section>

  <!-- ── 08 EARLY ACCESS ──────────────────────────────────────────────────── -->
  <section id="early-access" class="relative py-32 px-6 bg-surface border-y border-border overflow-hidden">
    <div aria-hidden="true" class="absolute top-0 left-1/2 -translate-x-1/2 w-[70%] h-80 bg-accent/[0.10] blur-3xl pointer-events-none"></div>
    <div class="relative max-w-2xl mx-auto">
      <div class="section-reveal text-center">
        <SectionEyebrow num="10" label="EARLY ACCESS" />
        <h2 class="text-3xl md:text-5xl font-black text-primary mb-4">Knot is in testing.</h2>
        <p class="text-secondary mb-10 leading-relaxed">
          We're not ready for a public release yet. The first wave of slots is for solo developers and 2–10 person teams who'd benefit <em class="text-primary">now</em> from removing the middleman from the loop. If that's you, we'd love to hear from you.
        </p>

        <a
          href="mailto:mail.roshankumargupta@gmail.com?subject=Knot%20early%20access&body=Hi%20—%20interested%20in%20Knot%20early%20access.%20A%20bit%20about%20me%2Fmy%20team%3A"
          class="inline-flex items-center gap-2 px-8 py-3.5 rounded-xl bg-accent text-bg font-bold text-sm hover:bg-white transition-all hover:scale-105 hover:shadow-[0_0_32px_rgba(0,212,170,0.4)]"
        >
          Request early access
          <span>→</span>
        </a>

        <p class="text-hint text-xs mt-6">
          Request access and we'll send you a testing build for macOS and Linux. It's pre-alpha and not yet notarized, so you'll wave it past your OS's first-run warning — the invite walks you through it.
        </p>
      </div>

      <!-- Honest framing card — same pattern as the chest-strap accuracy
           card on /heart/, but with credibility-by-honesty rather than
           credibility-by-spec. -->
      <div class="section-reveal mt-16 p-6 rounded-2xl bg-bg border border-border">
        <div class="flex items-start gap-4">
          <div class="shrink-0 w-10 h-10 rounded-xl bg-yellow-500/10 border border-yellow-500/30 flex items-center justify-center text-xl">⚠️</div>
          <div>
            <p class="text-yellow-500/90 text-xs font-mono tracking-widest mb-2">PRE-ALPHA</p>
            <h3 class="text-lg font-bold text-primary mb-2">What "in testing" actually means here.</h3>
            <p class="text-secondary text-sm leading-relaxed mb-3">
              The core sync loop works end-to-end across networks on real hardware. We've deliberately deferred a few things that don't matter for early-adopter use but will matter before general release:
            </p>
            <ul class="text-hint text-xs space-y-1.5 ml-1">
              <li>· Full three-way merge of diverged branches (the common single-file conflict already has a guided keep-mine / keep-theirs / keep-both flow; push still refuses on non-FF, resolve with `git` manually).</li>
              <li>· Polished onboarding wizard for non-developer users.</li>
              <li>· A public, notarized download (Apple Developer ID + Windows MSI). Until then, testing builds go out with the early-access invite — macOS + Linux today, Windows soon.</li>
            </ul>
            <p class="text-hint text-xs mt-3">
              If you're a developer comfortable with `git`, you'll feel the difference within the first hour. If you're not, wait for v1.1.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Base>
