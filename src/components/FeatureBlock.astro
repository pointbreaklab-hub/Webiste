---
export interface Props {
  icon: string;
  title: string;
  description: string;
}
const { icon, title, description } = Astro.props;
---
<div class="section-reveal flex flex-col gap-4 p-6 rounded-2xl bg-surface border border-border hover:border-accent/30 hover:-translate-y-0.5 hover:shadow-[0_6px_24px_-8px_rgba(0,212,170,0.25)] transition-all duration-300 group">
  <div class="w-10 h-10 rounded-xl bg-accent/10 flex items-center justify-center text-lg group-hover:bg-accent/20 transition-colors">
    {icon}
  </div>
  <h3 class="text-base font-bold text-primary">{title}</h3>
  <p class="text-secondary text-sm leading-relaxed">{description}</p>
</div>
